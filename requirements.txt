# Core
anthropic>=0.40.0
python-dotenv>=1.0.0

# Audio I/O
sounddevice>=0.4.6
numpy>=1.24.0

# Wake word detection
openwakeword>=0.6.0

# Voice activity detection (silence detection)
webrtcvad>=2.0.10

# Speech-to-text (fast CPU transcription)
faster-whisper>=1.0.0

# Text-to-speech (offline default)
pyttsx3>=2.90

# Optional: ElevenLabs TTS (only needed if you set ELEVENLABS_API_KEY)
elevenlabs>=1.0.0
